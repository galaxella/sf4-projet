{% extends '_template.html.twig' %}

{% block title %}Kikiriki - Inscription{% endblock %}

{% block content %}
    <div class="section">

        <h1 class="title">Inscription</h1>

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.name, { label: 'Nom'}) }}
            {{ form_row(registrationForm.lastname, { label: 'Prénom'}) }}
            {{ form_row(registrationForm.email, { label: 'Adresse mail'}) }}

            {{ form_row(registrationForm.plainPassword.first, {label: "Mot de passe" }) }}
            {{ form_row(registrationForm.plainPassword.second, { label:"Confirmer le mot de passe" }) }}

{#  {{ form_row(registrationForm.agreeTerms, { label: 'Vous acceptez les CGU'}) }} #}

            <button type="submit" class="btn">S'inscrire</button>
        {{ form_end(registrationForm) }}
    </div>
{% endblock %}
